<div id="content">
  <div class="content-title">New Books</div>
  <div class="admin-content-layer">
    <%= form_for([:admin, @book], :html => { :multipart => true}) do |f| %>
      <%= f.error_messages %>
      <%= render :partial => 'form', :locals => { :f => f} %>
      <div class="field">
        <%= f.submit 'Create Book' %>&nbsp;&nbsp;
        <%= link_to 'Back', admin_books_path %>
      </div>
    <% end %>
    <div class="clear"></div>
  </div>
</div>
