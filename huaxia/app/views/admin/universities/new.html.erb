<div id="content">
  <div class="content-title">New University</div>
  <div class="admin-content-layer">
    <%= form_for([:admin, @university], :html => { :multipart => true}) do |f| %>
      <%= f.error_messages %>
      <%= render :partial => 'form', :locals => { :f => f} %>
      <div class="field">
        <%= f.submit 'Create University' %>&nbsp;&nbsp;
        <%= link_to 'Back', admin_universities_path %>
      </div>
    <% end %>
    <div class="clear"></div>
  </div>
</div>
